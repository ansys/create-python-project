# We keep this file from the template to make sure /stubs directory is tracked by git
import sys
import os
from pathlib import Path
sys.path.insert(0, os.path.join(Path(os.path.dirname(os.path.realpath(__file__))).parent, 'stubs'))